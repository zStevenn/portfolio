import { shantell_sans } from "@/fonts"

function Logo() {
  return (
    <h1 className={`w-24 md:block ${shantell_sans.className} text-base md:text-lg lg:text-xl transition-all`}>Steven Li</h1>
  )
}

export default Logo